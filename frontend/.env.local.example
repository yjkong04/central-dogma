# URL of the locally-running FastAPI backend (server-side only).
API_BASE=http://localhost:8000

# Base URL of the deployed API (Lambda Function URL). Unset = same-origin /api/ask (dev proxy, removed in prod).
NEXT_PUBLIC_API_URL=

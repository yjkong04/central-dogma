# PDF fixtures

- `born_digital.pdf` — tiny text-layer PDF, regenerate via `make_fixtures.py`.
- `real_sample.pdf` — add your own real paper locally to run `-m slow` integration
  tests. Large binaries are not committed.

Set `CD_LAYOUT_MODEL` to a YOLO/DETR document-layout checkpoint to run the real pipeline.

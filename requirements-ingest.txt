# Self-hosted CV/ML deps for PDF ingestion (sub-project 1). Kept OUT of
# requirements.txt so the API image + default CI stay lean. Versions pinned
# in Task 3/Task 4 once the OCR + layout backends are chosen.
pypdfium2==4.30.0
Pillow==11.0.0
numpy==2.2.1
python-doctr[torch]==0.10.0
# 8.3.0 pins numpy<2.0.0, conflicting with the numpy==2.2.1 pin above (shared
# with requirements.txt). 8.3.203 drops that upper bound for the base install
# (kept only for the unused "export" extra) -- same public YOLO(...).predict()
# API, no code change needed.
ultralytics==8.3.203
